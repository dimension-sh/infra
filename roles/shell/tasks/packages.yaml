---
- name: Install Packages
  dnf:
    name: '{{ packages }}'
    state: present
  vars:
    packages:
      - ansible
      - firewalld
      - mutt
      - zip
      - unzip
      - git
      - tmux
      - nano
      - zsh