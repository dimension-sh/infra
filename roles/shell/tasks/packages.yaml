---
- name: Install Packages
  dnf:
    name: "{{ packages }}"
    state: present
  vars:
    packages:
      - alpine-maildir
      - ansible
      - bind-utils
      - figlet
      - git
      - lynx
      - mutt
      - nano
      - nmap-ncat
      - tmux
      - unzip
      - util-linux-user
      - wget
      - zip
      - zsh
- name: Install Python packages
  pip:
    name: "{{ packages }}"
  vars:
    packages:
      - toot
