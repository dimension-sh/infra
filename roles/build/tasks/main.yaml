- name: Install Packages
  dnf:
    name: "{{ packages }}"
    state: present
  vars:
    packages:
      - ansible
      - bind-utils
      - git
      - nano
      - nmap-ncat
      - unzip
      - wget
      - zip
      - gcc
      - automake
      - rpmdevtools
      - rpm-build